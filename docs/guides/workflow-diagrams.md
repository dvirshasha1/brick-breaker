# CI/CD Workflow Diagram & Architecture

## GitHub Actions Workflow

```
┌─────────────────────────────────────────────────────────────────┐
│                     GitHub Event Trigger                         │
│                  (Push or Pull Request)                          │
│                                                                  │
│         ↓ main ↓ develop ↓ feature/* ↓ bugfix/*                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                ┌───────────┴────────────┐
                ↓                        ↓
        ┌──────────────┐        ┌──────────────┐
        │ Quality      │        │ Tests        │
        │ Checks       │        │              │
        │ (Node 18.x & │        │ - Unit tests │
        │  20.x)       │        │ - Coverage   │
        │              │        │              │
        │ - TypeScript │        └──────┬───────┘
        │ - ESLint     │               │
        │ - Prettier   │               ↓ (Parallel)
        └──────┬───────┘        ┌──────────────┐
               │                │ Build        │
               │                │              │
               ↓                │ - Webpack    │
        ✅ All Pass             │ - Artifacts  │
               │                │              │
               └────────┬───────┴──────────────┘
                        │
                        ↓
                ┌────────────────┐
                │ Final Status   │
                │ all-checks-    │
                │ passed         │
                └────────┬───────┘
                         │
                    ✅ OR ❌
                         │
         ┌───────────────┴────────────────┐
         ↓                                ↓
    ✅ PR Ready              ❌ PR Blocked
    to Merge                 (Fix & Retry)
```

## Branch Protection Flow

```
┌─────────────────────────────────────────────────────────┐
│              Developer Creates PR                        │
│          (feature/* → develop/main)                     │
└───────────────┬─────────────────────────────────────────┘
                │
                ↓
        ┌───────────────────┐
        │ GitHub Triggers   │
        │ CI Pipeline       │
        └───────────┬───────┘
                    │
      ┌─────────────┴─────────────┐
      ↓                           ↓
   ✅ All Tests Pass          ❌ Tests Fail
      │                           │
      ↓                           ↓
  ┌────────────────┐      ┌──────────────┐
  │ Status Checks  │      │ Notify Dev   │
  │ Show Green     │      │ Fix Issues   │
  └────────┬───────┘      └──────┬───────┘
           │                     │
           ↓                     ↓
   ┌──────────────┐      Developer Pushes
   │ Require N    │      Fixed Code
   │ Approvals    │      │
   │ (1 for dev,  │      └──→ Re-runs CI
   │  2 for main) │
   └────────┬─────┘
            │
      ✅ Approved
            │
            ↓
    ┌──────────────┐
    │ All Checks   │
    │ Pass ✅      │
    │ Approval ✅  │
    │ Up-to-date ✅│
    └────────┬─────┘
             │
             ↓
    ┌──────────────┐
    │ MERGE        │
    │ ENABLED ✅   │
    └──────────────┘
```

## Job Dependency Graph

```
                          START
                            │
                ┌───────────┴───────────┐
                │                       │
                ↓                       ↓
        ┌─────────────────┐    ┌─────────────────┐
        │   Quality       │    │     Tests       │
        │   Checks        │    │                 │
        │ (Matrix 18/20)  │    │ - jest          │
        └────────┬────────┘    │ - coverage      │
                 │             └────────┬────────┘
                 │                      │
                 └──────────┬───────────┘
                            │
                            ↓
                    ┌───────────────┐
                    │    Build      │
                    │   (Webpack)   │
                    └────────┬──────┘
                             │
                             ↓
                    ┌──────────────────────┐
                    │  All-Checks-Passed   │
                    │  (Status Gate)       │
                    └────────┬─────────────┘
                             │
                         ✅ SUCCESS
                             │
        ┌────────────────────┴────────────────────┐
        │                                          │
        ↓                                          ↓
   Ready for Merge                         Available in PR
   if branch protection                   Checks section
   allows & approved
```

## CI Configuration Structure

```
┌─ .github/
│  └─ workflows/
│     └─ ci.yml ──────────────────┐
│                                 │
├─ .prettierrc ────────────────────┤
│                                 │
├─ .eslintrc.json ─────────────────┤
│                                 │
├─ jest.config.js ─────────────────┤
│                                 │
├─ tsconfig.json ──────────────────┤
│                                 │
├─ webpack.config.js ──────────────┤
│                                 │
└─ package.json (scripts) ─────────┴────→ CI Pipeline Execution
```

## Development Workflow

```
┌──────────────────────────────────────┐
│  1. Create Feature Branch             │
│     git checkout -b feature/name      │
└─────────────┬────────────────────────┘
              │
              ↓
┌──────────────────────────────────────┐
│  2. Implement & Test Locally          │
│     make test (17/17 passing)         │
└─────────────┬────────────────────────┘
              │
              ↓
┌──────────────────────────────────────┐
│  3. Pre-commit Checks                 │
│     make type-check                   │
│     make lint                         │
│     make format                       │
└─────────────┬────────────────────────┘
              │
              ↓
┌──────────────────────────────────────┐
│  4. Commit & Push                     │
│     git add . && git commit           │
│     git push origin feature/name      │
└─────────────┬────────────────────────┘
              │
              ↓
┌──────────────────────────────────────┐
│  5. Create Pull Request               │
│     develop ← feature/name            │
│     or main ← develop                 │
└─────────────┬────────────────────────┘
              │
              ↓
┌──────────────────────────────────────┐
│  6. GitHub Actions Runs               │
│     CI Pipeline (all jobs)            │
└─────────────┬────────────────────────┘
              │
        ┌─────┴─────┐
        ↓           ↓
    ✅ PASS     ❌ FAIL
        │           │
        ↓           ↓
   Ready to    Fix Issues
   Merge       (Repeat)
        │           │
        └─────┬─────┘
              ↓
        ✅ Approved
              │
              ↓
        ✅ Merged
              │
              ↓
      ✅ Deployment Ready
```

## Status Check Matrix

```
┌──────────────────────────────────────────────────────────────┐
│                Status Check Details                          │
├─────────────────────┬────────────────────────────────────────┤
│ Check Name          │ Purpose                                │
├─────────────────────┼────────────────────────────────────────┤
│ quality-check       │ TypeScript + ESLint + Prettier         │
│ (18.x)              │ on Node 18.x                           │
├─────────────────────┼────────────────────────────────────────┤
│ quality-check       │ TypeScript + ESLint + Prettier         │
│ (20.x)              │ on Node 20.x                           │
├─────────────────────┼────────────────────────────────────────┤
│ tests               │ Jest unit tests + Coverage reports     │
├─────────────────────┼────────────────────────────────────────┤
│ build               │ Webpack bundle creation                │
├─────────────────────┼────────────────────────────────────────┤
│ all-checks-passed   │ Final gate confirming all succeeded    │
└─────────────────────┴────────────────────────────────────────┘

Result Matrix:
┌──────────────┬──────────────┬────────────┬─────────┬──────────────┐
│ quality-18.x │ quality-20.x │ tests      │ build   │ Final Status │
├──────────────┼──────────────┼────────────┼─────────┼──────────────┤
│ ✅           │ ✅           │ ✅         │ ✅      │ ✅ PASS      │
│ ❌           │ ✅           │ ✅         │ ✅      │ ❌ FAIL      │
│ ✅           │ ✅           │ ❌         │ ✅      │ ❌ FAIL      │
│ ✅           │ ✅           │ ✅         │ ❌      │ ❌ FAIL      │
└──────────────┴──────────────┴────────────┴─────────┴──────────────┘
```

## Timeline: From Commit to Merge

```
Time    Event                                   Status
────────────────────────────────────────────────────────
T+0     Developer pushes code                   ⏳ Pending
        │
T+15s   GitHub receives push event              ⏳ Queued
        │ Triggers CI workflow
        │
T+30s   Quality checks start (2 parallel jobs)  🔄 Running
        Tests start                              🔄 Running
        │
T+2m    Quality checks complete                 ✅ Done
        Tests complete                          ✅ Done
        │ Triggers build job
T+2m15s Build starts                            🔄 Running
        │
T+3m    Build completes                         ✅ Done
        │ Triggers final status check
        │
T+3m15s All checks complete                     ✅ ALL PASS
        │
T+3m30s PR shows green checkmark                ✅ Approved
        │ If branch protection enabled
        │ and approvals met
        │
T+5m    Developer approves PR                   ✅ Ready
        (or admin auto-merges)
        │
T+6m    PR merged to develop/main               ✅ MERGED
```

## References

- **CI Configuration**: `.github/workflows/ci.yml`
- **Setup Guide**: `docs/guides/ci-setup.md`
- **Branch Protection**: `docs/guides/branch-protection-setup.md`
- **Status Checks**: 5 required checks (see matrix above)
